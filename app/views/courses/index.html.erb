
<div class="container-courses">
  <div class="header-section">
    <h6>Welcome to Par-tee, the ultimate pub golf game! Embark on a thrilling journey through each course, where every 'hole' is a unique pub with its own challenge. Choose from the exciting courses below, gather your friends, and let the fun begin! Who will emerge as the top golfer today?</h6>
  </div>

    <% @courses.each do |course| %>
      <div class="card-pub">
        <h6><%= link_to course.name, course_path(course), class:"card-pub-link" %></h6>
      <!--</div>

      <div class="card-map">-->
        <div style="width: 100%; height: 200px;"
          data-controller="map"
          data-map-markers-value="<%= params["#{course.name}"].to_json %>"
          data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    <% end %>

  <div class="d-grid gap-2">
    <%= link_to "Rate this app", ratings_path, class: "btn btn-primary" %>
  </div>

</div>
