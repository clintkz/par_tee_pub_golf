<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link href='https://api.mapbox.com/mapbox-gl-js/v2.6.1/mapbox-gl.css' rel='stylesheet' />
  <script src='https://api.mapbox.com/mapbox-gl-js/v2.6.1/mapbox-gl.js'></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css?family=Nunito:400,700|Work+Sans:400,700|Roboto:400,700&display=swap" rel="stylesheet">

  <style>
    h1, h2, h3, h4, h5, h6, p {
      font-family:  "Courier";
    }
  </style>
</head>

<div class="container-parent">
  <%= image_tag("logo_page.png", alt: "Par-tee Image", style: "width: 200px; height: auto;") %>
  <div class="container">
    <div class="card-heading">
      <h5>Welcome to Par-tee, the Pub golf drinking game! Choose from the courses below:</h5>
    </div>

    <% @courses.each do |course| %>
      <div class="card-pub">
        <p><%= course.name %> is in <%= course.location %> and has <%= course.number_of_pubs %> holes.  Happy drinking!</p>
      <div class="container-map">
        <div style="width: 100%; height: 100px;"
          data-controller="map"
          data-map-markers-value="<%= @markers.to_json %>"
          data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
      </div>
    <% end %>
  </div>
</div>
